#!/usr/bin/env bash

set -Eeuo pipefail
[[ $# -eq 1 && "$1" =~ ^[0-9]+$ ]] || { printf 'Usage: %s SNAPSHOT_ID\n' "${0##*/}" >&2; exit 2; }
sudo snapper -c root rollback "$1"
printf '%s\n' 'Rollback prepared. Reboot to enter the restored snapshot.'
